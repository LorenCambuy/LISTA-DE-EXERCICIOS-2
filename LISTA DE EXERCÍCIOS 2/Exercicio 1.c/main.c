#include <stdio.h>

int main() {
	
	//for(inicializacao; condicao; incremento/decremento) -> estrutura de repeticao	
	
	for(int contador=0; contador<=100; contador++){
		
		printf("%d \n", contador);		
	}	
	
	return 0;
}